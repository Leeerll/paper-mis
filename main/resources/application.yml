server:
  port: 9090

spring:
  datasource:
    driver-class-name=: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/liu?serverTimezone=GMT%2b8   //liu是数据库的名称
    username: root
    password: root123456

mybatis:
  mapper-locations: classpath:mapper/*.xml  #扫描所有mybatis的xml文件
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl